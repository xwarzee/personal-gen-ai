#!/bin/sh
# Stub de vllm pour les tests bats : journalise l'appel et réussit.
# Shebang /bin/sh (absolu) : fonctionne même sous un PATH de test restreint.
echo "vllm: $*" >> "${MOCK_LOG:-/dev/null}"
exit 0
